{
  "name": "api",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .dist",
    "watch": "webpack --watch",
    "predev": "yarn clean && webpack",
    "dev": "cd .dist && func extensions install && func start",
    "build": "yarn clean && webpack && rm -f local.settings.json"
  },
  "dependencies": {
    "@nvd.codes/config": "^1.0.0",
    "@nvd.codes/core": "^1.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "@nvd/tsconfig": "^1.0.0",
    "@types/node-fetch": "^2.5.7",
    "copy-webpack-plugin": "^6.3.1",
    "ts-loader": "^8.0.11",
    "typescript": "^4.0.5",
    "webpack": "^5.4.0",
    "webpack-cli": "^4.2.0"
  }
}
