{
  "name": "@nvd.codes/images",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .dist",
    "watch": "webpack --watch",
    "predev": "yarn build",
    "dev": "cd .dist && func extensions install && func start",
    "build": "yarn clean && webpack && yarn setup-sharp",
    "setup-sharp": "ts-node --project scripts/tsconfig.json scripts/copy-sharp-libs.ts --folder .dist",
    "start": "cd .dist && func start"
  },
  "dependencies": {
    "@nvd.codes/config": "^1.0.0",
    "@nvd.codes/core": "^1.0.0",
    "@azure/storage-blob": "^12.4.1",
    "@hapi/accept": "^5.0.2",
    "sharp": "^0.27.1"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.3",
    "@nvd/tsconfig": "^1.0.0",
    "copy-webpack-plugin": "^7.0.0",
    "node-loader": "^1.0.2",
    "ts-loader": "^8.0.17",
    "typescript": "^4.1.5",
    "webpack": "^5.21.2",
    "webpack-cli": "^4.5.0"
  }
}
